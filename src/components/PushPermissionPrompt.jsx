import { PushPermissionCard } from './PushPermissionCard'

/** My Meals prompt — hidden while checking or when notifications are enabled. */
export function PushPermissionPrompt() {
  return <PushPermissionCard variant="prompt" />
}
